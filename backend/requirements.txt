# Core orchestration
autogen-agentchat>=0.4.0
autogen-ext[openai]>=0.4.0

# WebSocket server
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
websockets>=14.0
python-multipart             # file uploads

# TTS (Microsoft Edge neural voices)
edge-tts

# HTTP client (for OpenRouter, Kie.ai, Deepgram)
aiohttp
requests

# Environment + logging
python-dotenv
loguru

# Memory (ChromaDB vector store for dual-layer agent memory)
chromadb

# File extraction (chat file sharing: PDF, DOCX, images)
PyPDF2
python-docx
pillow

# LiveKit WebRTC (real-time A/V transport + Deepgram STT)
livekit
livekit-api
